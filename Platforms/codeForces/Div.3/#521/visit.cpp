#include <iostream>
using namespace std;

int main() {
    int n;
    int a1,a2,a3,a4,T;

    cin >> T;
    int a[5];
    while (T--) {
        
        cin >> a[0] >> a[1] >> a[2] >> a[3];
    int cnt=0;
       for (int t=0;t<4;t++) {
           if (a[t]>0)
           cnt++;
       }

       
    }
}