#include <iostream>
#include <algorithm>
#define ll long long int
#define forn(t,n) for(t=0;t<n;t++)
using namespace std;

int main() {
  ll n,t;

  cin >> n;
  ll *a = new ll[n];
  bool e = false;
 
  forn(t,n) {
    cin >> a[t];
  }

  sort(a);
  
  /* if (n == 2) {
    cout << (a[1] - a[0]) / 2 << '\n';
  } else {
    for(t,n-1) {
      
    }
    
  }
  */
  forn(t,n) cout << a[t] << " ";
}
